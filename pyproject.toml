[project]
name = "nexpro"
version = "0.1.0"
description = "The Next Generation Programming Language"
authors = [
    {name = "Probal Dhali"}
]
requires-python = ">=3.11"

[project.scripts]
nexpro = "nexpro.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"